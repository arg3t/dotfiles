# Personal TTSR rules. Add .md files here.
# Each file's frontmatter controls TTSR behavior:
#   condition: regex trigger pattern
#   astCondition: ast-grep patterns
#   scope: text, thinking, tool, tool:name(glob)
#   interruptMode: always | prose-only | tool-only | never
#   globs: file-path gate for matching
#   alwaysApply: true (injects content instead of TTSR)
#   description: required for rulebook rules
